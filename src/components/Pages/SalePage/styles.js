const styles = theme => ({
  container: {
    marginBottom: 15,
  },
  title: {
    paddingTop: 20,
    paddingBottom: 20,
    fontSize: 15,
    color: theme.custom.text,
    fontWeight: 500,
  },
});

export default styles;
